{
  "$schema": "http://json-schema.org/schema",
  "$id": "NxCypressComponentProject",
  "cli": "nx",
  "title": "Set up Cypress component test for a project",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project to convert",
      "$default": {
        "$source": "projectName"
      }
    },
    "componentType": {
      "type": "string",
      "enum": ["react", "next"],
      "description": "Which framework is being used?"
    },
    "compiler": {
      "type": "string",
      "enum": ["swc", "babel"],
      "description": "Which compiler is being used in the existing project?",
      "default": "babel"
    },
    "force": {
      "type": "boolean",
      "description": "Overwrite an existing cypress component test project. This is helpful if you want to reset to the default configurations.",
      "default": false
    }
  },
  "required": ["project", "componentType"]
}
